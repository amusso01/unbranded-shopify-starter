{% doc %}
  Loads all font variations with display: swap for header and body fonts.

  @example
  {% render 'font-loader' %}
{% enddoc %}

{% style -%}
{% # Load default header font (only if no alternative font is set) -%}
{%- unless settings.header_alternative_font_name != blank -%}
{{ settings.type_header_font | font_face: font_display: 'swap' }}
{{ settings.type_header_font | font_modify: 'weight', 'bold' | font_face: font_display: 'swap' }}
{{ settings.type_header_font | font_modify: 'weight', 'bold' | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }}
{{ settings.type_header_font | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }}
{%- endunless -%}
{% # Load default body font (only if no alternative font is set) -%}
{%- unless settings.body_alternative_font_name != blank -%}
{{ settings.type_body_font | font_face: font_display: 'swap' }}
{{ settings.type_body_font | font_modify: 'weight', 'bold' | font_face: font_display: 'swap' }}
{{ settings.type_body_font | font_modify: 'weight', 'bold' | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }}
{{ settings.type_body_font | font_modify: 'style', 'italic' | font_face: font_display: 'swap' }}
{%- endunless -%}

  {% # ============================================ %}
  {% # Custom Font Loading - Header Alternative Font %}
  {% # ============================================ %}
  {% if settings.header_alternative_font_name != blank %}
    {% liquid
      assign header_font_name = settings.header_alternative_font_name | strip
      assign is_variable = settings.header_alternative_font_variable
    %}
    
    {% # Build src list for variable fonts (same file for all weights) %}
    {% if is_variable %}
      {% liquid
        assign src_list = ''
        assign has_extension = false
        
        if settings.header_alternative_font_woff2
          assign has_extension = true
          assign font_file = header_font_name | append: '.woff2'
          assign font_url = font_file | asset_url
          if src_list != blank
            assign src_list = src_list | append: ', '
          endif
          assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
        endif
        
        if settings.header_alternative_font_woff
          assign has_extension = true
          assign font_file = header_font_name | append: '.woff'
          assign font_url = font_file | asset_url
          if src_list != blank
            assign src_list = src_list | append: ', '
          endif
          assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
        endif
        
        if settings.header_alternative_font_ttf
          assign has_extension = true
          assign font_file = header_font_name | append: '.ttf'
          assign font_url = font_file | asset_url
          if src_list != blank
            assign src_list = src_list | append: ', '
          endif
          assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
        endif
        
        if settings.header_alternative_font_eot
          assign has_extension = true
          assign font_file = header_font_name | append: '.eot'
          assign font_url = font_file | asset_url
          if src_list != blank
            assign src_list = src_list | append: ', '
          endif
          assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
        endif
      %}
      {% if has_extension %}
        {% # Generate normal weight @font-face declarations %}
        {%- if settings.header_alternative_font_weight_100 -%}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:100;font-style:normal;}
{%- endif -%}
        {%- if settings.header_alternative_font_weight_100_italic -%}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:100;font-style:italic;}
{%- endif -%}
{%- if settings.header_alternative_font_weight_200 -%}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:200;font-style:normal;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_200_italic -%}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:200;font-style:italic;}
{%- endif -%}
{%- if settings.header_alternative_font_weight_300 -%}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:300;font-style:normal;}
{%- endif -%}
{%- if settings.header_alternative_font_weight_300_italic %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:300;font-style:italic;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_400 %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:400;font-style:normal;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_400_italic %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:400;font-style:italic;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_500 %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:500;font-style:normal;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_500_italic %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:500;font-style:italic;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_600 %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:600;font-style:normal;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_600_italic %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:600;font-style:italic;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_700 %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:700;font-style:normal;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_700_italic %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:700;font-style:italic;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_800 %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:800;font-style:normal;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_800_italic %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:800;font-style:italic;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_900 %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:900;font-style:normal;}
        {%- endif -%}
{%- if settings.header_alternative_font_weight_900_italic %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:900;font-style:italic;}
{%- endif -%}
{%- endif -%}
    {% else %}
      {% # Non-variable font: different file per weight (FontName-{weight}.woff2) %}
      {% # Generate normal weight @font-face declarations %}
      {% if settings.header_alternative_font_weight_100 %}
        {% liquid
          assign weight_suffix = '-' | append: '100'
          assign src_list = ''
          assign has_extension = false
          
          if settings.header_alternative_font_woff2
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          
          if settings.header_alternative_font_woff
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          
          if settings.header_alternative_font_ttf
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          
          if settings.header_alternative_font_eot
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
        {% if has_extension %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:100;font-style:normal;}
        {% endif %}
{%- endif -%}
{%- if settings.header_alternative_font_weight_100_italic %}
        {% liquid
          assign weight_suffix = '-' | append: '100-Italic'
          assign src_list = ''
          assign has_extension = false
          
          if settings.header_alternative_font_woff2
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          
          if settings.header_alternative_font_woff
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          
          if settings.header_alternative_font_ttf
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          
          if settings.header_alternative_font_eot
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
        {% if has_extension %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:100;font-style:italic;}
{%- endif -%}
{%- endif -%}
      {% # Repeat for weights 200-900 (abbreviated for space, but same pattern) %}
      {% if settings.header_alternative_font_weight_200 %}
        {% liquid
          assign weight_suffix = '-' | append: '200'
          assign src_list = ''
          assign has_extension = false
          if settings.header_alternative_font_woff2
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.header_alternative_font_woff
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.header_alternative_font_ttf
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.header_alternative_font_eot
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
        {% if has_extension %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:200;font-style:normal;}
        {% endif %}
{%- endif -%}
{%- if settings.header_alternative_font_weight_200_italic -%}
        {% liquid
          assign weight_suffix = '-' | append: '200-Italic'
          assign src_list = ''
          assign has_extension = false
          if settings.header_alternative_font_woff2
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.header_alternative_font_woff
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.header_alternative_font_ttf
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.header_alternative_font_eot
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
        {% if has_extension %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:200;font-style:italic;}
{%- endif -%}
{%- endif -%}
      {% # Generate @font-face for weights 300-900 %}
      {% if settings.header_alternative_font_weight_300 %}
        {% liquid
          assign weight_suffix = '-' | append: '300'
          assign src_list = ''
          assign has_extension = false
          if settings.header_alternative_font_woff2
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.header_alternative_font_woff
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.header_alternative_font_ttf
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.header_alternative_font_eot
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
        {% if has_extension %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:300;font-style:normal;}
        {% endif %}
{%- endif -%}
{%- if settings.header_alternative_font_weight_300_italic %}
        {% liquid
          assign weight_suffix = '-' | append: '300-Italic'
          assign src_list = ''
          assign has_extension = false
          if settings.header_alternative_font_woff2
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.header_alternative_font_woff
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.header_alternative_font_ttf
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.header_alternative_font_eot
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
        {% if has_extension %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:300;font-style:italic;}
{%- endif -%}
{%- endif -%}
      {% # Continue for 400-900 (using same pattern) %}
      {% if settings.header_alternative_font_weight_400 %}
        {% liquid
          assign weight_suffix = '-' | append: '400'
          assign src_list = ''
          assign has_extension = false
          if settings.header_alternative_font_woff2
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.header_alternative_font_woff
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.header_alternative_font_ttf
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.header_alternative_font_eot
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
        {% if has_extension %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:400;font-style:normal;}
        {% endif %}
{%- endif -%}
{%- if settings.header_alternative_font_weight_400_italic %}
        {% liquid
          assign weight_suffix = '-' | append: '400-Italic'
          assign src_list = ''
          assign has_extension = false
          if settings.header_alternative_font_woff2
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.header_alternative_font_woff
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.header_alternative_font_ttf
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.header_alternative_font_eot
            assign has_extension = true
            assign font_file = header_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
        {% if has_extension %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:400;font-style:italic;}
{%- endif -%}
{%- endif -%}
      {% # Generate remaining weights 500-900 (same pattern as above) %}
      {% assign remaining_weights = '500,600,700,800,900' | split: ',' %}
      {% for weight in remaining_weights %}
        {% case weight %}
          {% when '500' %}
            {% assign weight_selected = settings.header_alternative_font_weight_500 %}
            {% assign italic_selected = settings.header_alternative_font_weight_500_italic %}
          {% when '600' %}
            {% assign weight_selected = settings.header_alternative_font_weight_600 %}
            {% assign italic_selected = settings.header_alternative_font_weight_600_italic %}
          {% when '700' %}
            {% assign weight_selected = settings.header_alternative_font_weight_700 %}
            {% assign italic_selected = settings.header_alternative_font_weight_700_italic %}
          {% when '800' %}
            {% assign weight_selected = settings.header_alternative_font_weight_800 %}
            {% assign italic_selected = settings.header_alternative_font_weight_800_italic %}
          {% when '900' %}
            {% assign weight_selected = settings.header_alternative_font_weight_900 %}
            {% assign italic_selected = settings.header_alternative_font_weight_900_italic %}
        {% endcase %}
        {% if weight_selected %}
          {% liquid
            assign weight_suffix = '-' | append: weight
            assign src_list = ''
            assign has_extension = false
            if settings.header_alternative_font_woff2
              assign has_extension = true
              assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
            endif
            if settings.header_alternative_font_woff
              assign has_extension = true
              assign font_file = header_font_name | append: weight_suffix | append: '.woff'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
            endif
            if settings.header_alternative_font_ttf
              assign has_extension = true
              assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
            endif
            if settings.header_alternative_font_eot
              assign has_extension = true
              assign font_file = header_font_name | append: weight_suffix | append: '.eot'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
            endif
          %}
          {% if has_extension %}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:{{ weight }};font-style:normal;}
{%- endif -%}
{%- endif -%}
{%- if italic_selected -%}
{% liquid
            assign weight_suffix = '-' | append: weight | append: '-Italic'
            assign src_list = ''
            assign has_extension = false
            if settings.header_alternative_font_woff2
              assign has_extension = true
              assign font_file = header_font_name | append: weight_suffix | append: '.woff2'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
            endif
            if settings.header_alternative_font_woff
              assign has_extension = true
              assign font_file = header_font_name | append: weight_suffix | append: '.woff'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
            endif
            if settings.header_alternative_font_ttf
              assign has_extension = true
              assign font_file = header_font_name | append: weight_suffix | append: '.ttf'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
            endif
            if settings.header_alternative_font_eot
              assign has_extension = true
              assign font_file = header_font_name | append: weight_suffix | append: '.eot'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
            endif
          %}
{%- if has_extension -%}
@font-face{font-family:'{{ settings.header_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:{{ weight }};font-style:italic;}
{%- endif -%}
        {% endif %}
      {% endfor %}
    {% endif %}
  {% endif %}

  {% # ============================================ %}
  {% # Custom Font Loading - Body Alternative Font %}
  {% # ============================================ %}
  {% if settings.body_alternative_font_name != blank %}
    {% liquid
      assign body_font_name = settings.body_alternative_font_name | strip
      assign is_variable = settings.body_alternative_font_variable
    %}
    
    {% # Build src list for variable fonts (same file for all weights) %}
    {% if is_variable %}
      {% liquid
        assign src_list = ''
        assign has_extension = false
        
        if settings.body_alternative_font_woff2
          assign has_extension = true
          assign font_file = body_font_name | append: '.woff2'
          assign font_url = font_file | asset_url
          if src_list != blank
            assign src_list = src_list | append: ', '
          endif
          assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
        endif
        
        if settings.body_alternative_font_woff
          assign has_extension = true
          assign font_file = body_font_name | append: '.woff'
          assign font_url = font_file | asset_url
          if src_list != blank
            assign src_list = src_list | append: ', '
          endif
          assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
        endif
        
        if settings.body_alternative_font_ttf
          assign has_extension = true
          assign font_file = body_font_name | append: '.ttf'
          assign font_url = font_file | asset_url
          if src_list != blank
            assign src_list = src_list | append: ', '
          endif
          assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
        endif
        
        if settings.body_alternative_font_eot
          assign has_extension = true
          assign font_file = body_font_name | append: '.eot'
          assign font_url = font_file | asset_url
          if src_list != blank
            assign src_list = src_list | append: ', '
          endif
          assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
        endif
      %}
{%- if has_extension -%}
{% # Generate normal and italic weight @font-face declarations -%}
{%- if settings.body_alternative_font_weight_100 -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:100;font-style:normal;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_100_italic -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:100;font-style:italic;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_200 -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:200;font-style:normal;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_200_italic -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:200;font-style:italic;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_300 -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:300;font-style:normal;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_300_italic -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:300;font-style:italic;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_400 -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:400;font-style:normal;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_400_italic -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:400;font-style:italic;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_500 -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:500;font-style:normal;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_500_italic -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:500;font-style:italic;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_600 -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:600;font-style:normal;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_600_italic -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:600;font-style:italic;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_700 -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:700;font-style:normal;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_700_italic -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:700;font-style:italic;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_800 -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:800;font-style:normal;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_800_italic -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:800;font-style:italic;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_900 -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:900;font-style:normal;}
{%- endif -%}
{%- if settings.body_alternative_font_weight_900_italic -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:900;font-style:italic;}
{%- endif -%}
{%- endif -%}
{%- else -%}
      {% # Non-variable font: different file per weight (FontName-{weight}.woff2) %}
      {% # Generate @font-face for weights 100-900 with normal and italic styles %}
{%- if settings.body_alternative_font_weight_100 -%}
{% liquid
          assign weight_suffix = '-' | append: '100'
          assign src_list = ''
          assign has_extension = false
          if settings.body_alternative_font_woff2
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.body_alternative_font_woff
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.body_alternative_font_ttf
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.body_alternative_font_eot
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
%}
{%- if has_extension -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:100;font-style:normal;}
{%- endif -%}
{%- endif -%}
{%- if settings.body_alternative_font_weight_100_italic -%}
        {% liquid
          assign weight_suffix = '-' | append: '100-Italic'
          assign src_list = ''
          assign has_extension = false
          if settings.body_alternative_font_woff2
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.body_alternative_font_woff
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.body_alternative_font_ttf
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.body_alternative_font_eot
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
{%- if has_extension -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:100;font-style:italic;}
{%- endif -%}
{%- endif -%}
      {% # Generate remaining weights 200-900 using same pattern as header font %}
{%- if settings.body_alternative_font_weight_200 -%}
        {% liquid
          assign weight_suffix = '-' | append: '200'
          assign src_list = ''
          assign has_extension = false
          if settings.body_alternative_font_woff2
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.body_alternative_font_woff
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.body_alternative_font_ttf
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.body_alternative_font_eot
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
{%- if has_extension -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:200;font-style:normal;}
        {% endif %}
{%- endif -%}
{%- if settings.body_alternative_font_weight_200_italic %}
        {% liquid
          assign weight_suffix = '-' | append: '200-Italic'
          assign src_list = ''
          assign has_extension = false
          if settings.body_alternative_font_woff2
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.woff2'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
          endif
          if settings.body_alternative_font_woff
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.woff'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
          endif
          if settings.body_alternative_font_ttf
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.ttf'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
          endif
          if settings.body_alternative_font_eot
            assign has_extension = true
            assign font_file = body_font_name | append: weight_suffix | append: '.eot'
            assign font_url = font_file | asset_url
            if src_list != blank
              assign src_list = src_list | append: ', '
            endif
            assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
          endif
        %}
{%- if has_extension -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:200;font-style:italic;}
{%- endif -%}
{%- endif -%}
      {% # Generate weights 300-900 using loop pattern %}
      {% assign remaining_weights = '300,400,500,600,700,800,900' | split: ',' %}
      {% for weight in remaining_weights %}
        {% case weight %}
          {% when '300' %}
            {% assign weight_selected = settings.body_alternative_font_weight_300 %}
            {% assign italic_selected = settings.body_alternative_font_weight_300_italic %}
          {% when '400' %}
            {% assign weight_selected = settings.body_alternative_font_weight_400 %}
            {% assign italic_selected = settings.body_alternative_font_weight_400_italic %}
          {% when '500' %}
            {% assign weight_selected = settings.body_alternative_font_weight_500 %}
            {% assign italic_selected = settings.body_alternative_font_weight_500_italic %}
          {% when '600' %}
            {% assign weight_selected = settings.body_alternative_font_weight_600 %}
            {% assign italic_selected = settings.body_alternative_font_weight_600_italic %}
          {% when '700' %}
            {% assign weight_selected = settings.body_alternative_font_weight_700 %}
            {% assign italic_selected = settings.body_alternative_font_weight_700_italic %}
          {% when '800' %}
            {% assign weight_selected = settings.body_alternative_font_weight_800 %}
            {% assign italic_selected = settings.body_alternative_font_weight_800_italic %}
          {% when '900' %}
            {% assign weight_selected = settings.body_alternative_font_weight_900 %}
            {% assign italic_selected = settings.body_alternative_font_weight_900_italic %}
        {% endcase %}
        {% if weight_selected %}
          {% liquid
            assign weight_suffix = '-' | append: weight
            assign src_list = ''
            assign has_extension = false
            if settings.body_alternative_font_woff2
              assign has_extension = true
              assign font_file = body_font_name | append: weight_suffix | append: '.woff2'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
            endif
            if settings.body_alternative_font_woff
              assign has_extension = true
              assign font_file = body_font_name | append: weight_suffix | append: '.woff'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
            endif
            if settings.body_alternative_font_ttf
              assign has_extension = true
              assign font_file = body_font_name | append: weight_suffix | append: '.ttf'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
            endif
            if settings.body_alternative_font_eot
              assign has_extension = true
              assign font_file = body_font_name | append: weight_suffix | append: '.eot'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
            endif
          %}
  {%- if has_extension -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:{{ weight }};font-style:normal;}
{%- endif -%}
{%- endif -%}
{%- if italic_selected -%}
{% liquid
            assign weight_suffix = '-' | append: weight | append: '-Italic'
            assign src_list = ''
            assign has_extension = false
            if settings.body_alternative_font_woff2
              assign has_extension = true
              assign font_file = body_font_name | append: weight_suffix | append: '.woff2'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff2')"
            endif
            if settings.body_alternative_font_woff
              assign has_extension = true
              assign font_file = body_font_name | append: weight_suffix | append: '.woff'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('woff')"
            endif
            if settings.body_alternative_font_ttf
              assign has_extension = true
              assign font_file = body_font_name | append: weight_suffix | append: '.ttf'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('truetype')"
            endif
            if settings.body_alternative_font_eot
              assign has_extension = true
              assign font_file = body_font_name | append: weight_suffix | append: '.eot'
              assign font_url = font_file | asset_url
              if src_list != blank
                assign src_list = src_list | append: ', '
              endif
              assign src_list = src_list | append: "url('" | append: font_url | append: "') format('embedded-opentype')"
            endif
          %}
{%- if has_extension -%}
@font-face{font-family:'{{ settings.body_alternative_font_name }}';src:{{ src_list }};font-display:swap;font-weight:{{ weight }};font-style:italic;}
{%- endif -%}
{%- endif -%}
{%- endfor -%}
{%- endif -%}
{%- endif -%}
{%- endstyle -%}
